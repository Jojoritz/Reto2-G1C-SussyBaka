package client.beans;

/**
 * This is the user status enumeration
 * @author GAME
 */
public enum UserStatus {
    /**
     * The user is enabled, so he can use the application
     */
    ENABLED,
    /**
     * The user is disable, so he can not use the application
     */
    DISABLED
}
